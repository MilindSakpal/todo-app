
  # Build with previous prompt

  This is a code bundle for Build with previous prompt. The original project is available at https://www.figma.com/design/eWv60NhoXDhAYcerp8aHTE/Build-with-previous-prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  